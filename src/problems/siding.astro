<div class="siding-takeover">
  <div class="plate">
    <div class="plate-inner">
      <div class="plate-rivets top">
        <span class="rivet"></span><span class="rivet"></span><span class="rivet"></span><span class="rivet"></span>
      </div>
      <p class="plate-label">SIDING</p>
      <div class="plate-rule"></div>
      <p class="plate-sub">trainspotting, collected.</p>
      <div class="plate-rivets bottom">
        <span class="rivet"></span><span class="rivet"></span><span class="rivet"></span><span class="rivet"></span>
      </div>
    </div>
  </div>
  <div class="specs">
    <div class="spec">
      <span class="spec-val">UIC</span>
      <span class="spec-key">Number Scanner</span>
    </div>
    <div class="spec">
      <span class="spec-val">MAP</span>
      <span class="spec-key">Sighting Log</span>
    </div>
    <div class="spec">
      <span class="spec-val">iOS</span>
      <span class="spec-key">Platform</span>
    </div>
  </div>
  <p class="pitch">Scan the number. Snap the photo.<br>See everywhere you've been.</p>
  <div class="indicator">
    <span class="light red"></span>
    <span class="light-label">in development</span>
  </div>
</div>

<style is:inline>
  .siding-takeover {
    max-width: 380px;
    width: 100%;
    text-align: center;
  }
  .plate {
    background: linear-gradient(175deg, #3a3835 0%, #2e2c2a 30%, #252422 60%, #333130 100%);
    border-radius: 6px;
    padding: 3px;
    margin-bottom: 2rem;
    box-shadow:
      0 2px 8px rgba(0,0,0,0.4),
      inset 0 1px 0 rgba(255,255,255,0.08);
  }
  .plate-inner {
    border: 2px solid rgba(255,255,255,0.06);
    border-radius: 4px;
    padding: 1.5rem 1.5rem;
    background: linear-gradient(180deg, #302e2c 0%, #282624 100%);
    position: relative;
  }
  .plate-rivets {
    display: flex;
    justify-content: space-between;
    padding: 0 0.25rem;
  }
  .plate-rivets.top { margin-bottom: 1rem; }
  .plate-rivets.bottom { margin-top: 1rem; }
  .rivet {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: radial-gradient(circle at 35% 35%, #4a4845, #2a2826);
    box-shadow: inset 0 1px 2px rgba(0,0,0,0.4), 0 1px 0 rgba(255,255,255,0.05);
  }
  .plate-label {
    font-family: 'IBM Plex Mono', 'SF Mono', monospace;
    font-size: 2.8rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: #e0dcd6;
    text-shadow: 0 1px 0 rgba(0,0,0,0.4);
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  .plate-rule {
    width: 60px;
    height: 2px;
    background: #c13028;
    margin: 0 auto 0.5rem;
    box-shadow: 0 1px 0 rgba(255,255,255,0.05);
  }
  .plate-sub {
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    color: #8a8580;
    font-weight: 500;
  }
  .specs {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-bottom: 2rem;
  }
  .spec {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .spec-val {
    font-family: 'IBM Plex Mono', 'SF Mono', monospace;
    font-size: 1rem;
    font-weight: 700;
    color: #c13028;
    letter-spacing: 0.05em;
  }
  .spec-key {
    font-size: 0.6rem;
    opacity: 0.35;
    letter-spacing: 0.08em;
    margin-top: 0.2rem;
  }
  .pitch {
    font-size: 0.9rem;
    line-height: 1.6;
    opacity: 0.5;
    margin-bottom: 2rem;
  }
  .indicator {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }
  .light.red {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #c13028;
    box-shadow: 0 0 6px #c13028;
    animation: siding-blink 2s ease-in-out infinite;
  }
  @keyframes siding-blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }
  .light-label {
    font-family: 'IBM Plex Mono', 'SF Mono', monospace;
    font-size: 0.6rem;
    letter-spacing: 0.15em;
    opacity: 0.3;
  }
</style>
